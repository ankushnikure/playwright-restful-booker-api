## Project Setup

```bash
mkdir playwright-restful-booker-api-framework
cd playwright-restful-booker-api-framework
npm init -y
npm install -D @playwright/test
npx playwright install
npm install -D typescript @types/node
npx tsc --init
npm install dotenv
npm install -D @faker-js/faker allure-playwright
```